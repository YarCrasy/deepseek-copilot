import type { TranslationCatalog } from "../Types";

export const common = {
  navigation: {
    history: "History",
    back: "Back",
    newChat: "New Chat",
    settings: "Settings"
  }
} satisfies TranslationCatalog;
